[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build -- --configuration production"]

[start]
cmd = "npx serve -s dist/electro-ecom/browser -l ${PORT:-3000}"
